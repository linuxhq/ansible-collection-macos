---
bugfixes:
  - privoxy - support check mode on hosts where the package is not yet
    installed; the service tasks and restart handler no longer fail probing
    a missing formula during a dry run.
trivial:
  - privoxy - add a molecule scenario running the role against a Tart macOS
    virtual machine via the vagrant driver.
  - privoxy - simplify the configuration template with an ``onoff`` macro and
    by dropping redundant guards around loops; rendered output is unchanged.
...
