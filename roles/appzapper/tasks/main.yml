---
- name: Ensure appzapper package is managed
  tags:
    - appzapper
  community.general.homebrew_cask:
    name: "{{ appzapper_package }}"
    state: "{{ appzapper_state }}"
    update_homebrew: false
    upgrade_all: false

- name: Ensure appzapper defaults are managed
  tags:
    - appzapper
  community.general.osx_defaults:
    array_add: "{{ _default.array_add | d(omit) }}"
    domain: "{{ appzapper_domain }}"
    host: "{{ _default.host | d(omit) }}"
    key: "{{ _default.key }}"
    state: "{{ _default.state | d('present') }}"
    type: "{{ _default.type | d('string') }}"
    value: "{{ _default.value | d(omit) }}"
  loop: "{{ appzapper_defaults }}"
  loop_control:
    label: "{{ _default.key | d(none) }}"
    loop_var: _default
  when:
    - _default.key is defined
    - _default.value is defined or
      _default.state | d('present') != 'present'
...
