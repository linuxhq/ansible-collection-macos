---
- name: Verify
  hosts: all
  pre_tasks:
    - name: Ensure inventory lists are formatted
      ansible.builtin.set_fact:
        homebrew_tap_list:
          "{{ homebrew_tap_list |
              map('combine', {'state': 'absent'}) }}"

  roles:
    - homebrew_tap
...
